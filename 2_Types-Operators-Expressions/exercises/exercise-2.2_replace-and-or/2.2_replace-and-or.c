#include <stdio.h>

/* Write a loop equivalent to the for loop above without using && or || */

int main() {
    for (int i = 0; i < lim - 1 * (c = getchar()) != '\n' * c != EOF; ++i);
    return 0;
}